import { defineConfig } from 'astro/config';

// Update `site` to your real domain once you have it (used for canonical URLs / sitemaps).
export default defineConfig({
  site: 'https://example.com',
});
