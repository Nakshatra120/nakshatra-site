node_modules
dist
.astro
.DS_Store

# raw source for the portrait (cropped version lives in src/assets)
linkedin_img.jpeg

# local, machine-specific Claude Code permissions — keep private
.claude/settings.local.json
